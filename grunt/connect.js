module.exports = {
    main: {
        options: {
            hostname: "localhost",
            port: "8080",
            base: '<%= paths.dist %>/server',
            livereload: true,
            open: true
        }
    },
};